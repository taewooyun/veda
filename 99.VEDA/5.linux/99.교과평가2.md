## 교과평가

### SPR r14 LR
### Startup code
    - .global _start

    ```
    _start:
            mov r1, #10       ; count = 10 EA
            mov r2, #0        ; sum
    L1:
            add r2, r2, r1    ; r2 = r2 + r1
            subs r1, r1, #1    ; 10 -> 9 -> 8 ... -> 0 // ALU -report -> Flag Z=1 
            ; report를 하려면 명령어에 s를 붙여야함
            bNE L1

            mov r0, r2        ; r0 <- r2
    ```

    해석을 해야함

### Instruction 종류와 해석
  - ALU
  - ADR
  - LDR

### timer setting
    84MHz 나누는 수 설정

### 메모리 맵
  - cs (chip select) 2비트 칩셀렉트
    - A7(cs)
  - 접근하는 방법

### CPU 파이프라인
- 3way pipeline
- IF ID IE

- 5way pipeline
- IF ID IE MA(Memory Access) RW(Register Write)

- 순서정도

### BUS
- Address bus
- data bus
- control bus


### symbolic link
- inode
- 하드링크 소프트 링크
- 출력했을 때 같은 값이 나오는것과 그렇지 않은것

### ps -l
- PID PPID 개념
- `$ pstree` : pid ppid 간의 가계 확인

### 접근 권한
- `$ chmod`


### 조건문과 반복문
- if
- case
- for
- while

### 쉘 스크립트 표현식? 확장?
[] : 조건
  - -lt

{} : 
() :
'' : 문자열 변환
"" : 변수 치환?
let "" : 
expr : 
(()) : 코드 문법

### 변수 사용
`$ echo $var` # eval : 변수 값 나옴
`$ echo ${var}` # eval : 변수 값 나옴
`$ echo "$var"` # eval : 변수 값 나옴
`$ echo '$var'` # literal : 문자열 그대로 나옴

### SIGNALL & Kill Process
`$ sleep &` : 백그라운드 실행
`$ kill $PID`
`$ kill %3` : 3번 프로세스 종료
`$ kill -SIGKILL %3 ` : 3번 프로세스 KILL
`$ kill -l`
`$ killall`
`$ jobs`




### 추가?
1. linux env
2. bash shell
3. command
4. gcc
5. gdb
6. lib
  - .a .so .so
7. shell script
  - language 

#### two types of computer languages
1. compile
  ie, c, c++
2. interpreter
  - 상대적으로 속도가 느림
  - 사용이 쉬움
  ie, on-the-fly
  ex) python / java script / bash shell script








---

## Review

### CPU와 SoC
cpu : ALU 기반, FPU, cache 
      reg = {r0, r1, ...} // GPR : scrach pad reg. 연산을 하는동안 임시로 사용하는 값을 저장
SoC : cpu + devices

ALU의 연산을 report하면 flag(NZCV)에 상태를 저장함
N : 음수
Z : 0
C : 캐리 발생
V : 오버플로우

### 임베디드 메모리 맵 접근
memory mapped I/O 장치
  - SFR에 접근하여 조작

### 리눅스 하드링크 소프트링크 관계

### PID와 PPID
`$ ps -l`

### kill process
kill %2
kill {PID}
killall {CMD}

### grep

### find

### tar
`$ tar -cf files.tar a b c d` # tar 생성

`$ tar -tf files.tar` # tar 내용물 열람

`$ gzip files.tar`

`$ tar -cvzf file.tgz a b c d` : 파일 압축 생성

`$ tar -xvzf files.tar.gz` : 파일 압축 풀기

### sh
```bash
#!/bin/bash

s=0
i=0

while [ $i -lt 10 ] # 동일 의미
until [ $i -gt 10 ] # 동일 의미
do
    s+=i
done
    ((s+=i))
    let "s+=i"

    ((i++))
echo "sum from 10 to 10 is $s"
```

```bash


```


