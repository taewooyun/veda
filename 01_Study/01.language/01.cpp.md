### 이동 생성자
2011 ~ : modern C++

copy constructor : 복사 생성자
move constructor : 이동 생성자

`Mat m1 = m2`
`Mat m1(m2)`
`Mat m1{m2}`

### Functor
```cpp
A = imread("d:/lenna.jpg", IMREAD_COLOR);
Rectr(10, 10, 100, 100);
Mat D = A(r); // functor
```

C++에서 **Functor(펑터)** 는
👉 **객체처럼 보이지만 함수처럼 호출할 수 있는 것** 을 말합니다.

핵심은 **`operator()` 를 오버로딩한 클래스/구조체**입니다.

---

## 1️⃣ Functor란?

일반 함수 호출:

```cpp
int add(int a, int b) {
    return a + b;
}
add(1, 2);
```

Functor 호출:

```cpp
Adder adder;
adder(1, 2);   // 함수처럼 호출됨
```

👉 **객체지만 함수 호출 연산자 `()`를 가지고 있음**

---

## 2️⃣ 기본 Functor 예제

```cpp
#include <iostream>
using namespace std;

struct Adder {
    int operator()(int a, int b) {
        return a + b;
    }
};

int main() {
    Adder add;
    cout << add(3, 4);  // 7
}
```

✔ `Adder`는 객체
✔ `add(3,4)`는 함수 호출처럼 동작
✔ 내부적으로 `add.operator()(3,4)` 호출

---

## 3️⃣ Functor의 가장 큰 장점

### ✅ 상태(state)를 가질 수 있음

```cpp
struct Counter {
    int count = 0;

    int operator()() {
        return ++count;
    }
};

int main() {
    Counter c;
    cout << c(); // 1
    cout << c(); // 2
    cout << c(); // 3
}
```

👉 일반 함수는 **상태 저장이 어렵지만**, functor는 **멤버 변수로 상태 유지 가능**

---

## 4️⃣ 함수 포인터 vs Functor

| 구분      | 함수 포인터 | Functor |
| ------- | ------ | ------- |
| 상태 저장   | ❌      | ✅       |
| 인라인 최적화 | 제한적    | 유리      |
| 확장성     | 낮음     | 높음      |
| 객체지향    | ❌      | ✅       |

---

## 5️⃣ STL에서 Functor 사용 예

### 🔹 `std::sort` + Functor

```cpp
#include <algorithm>
#include <vector>
using namespace std;

struct Desc {
    bool operator()(int a, int b) {
        return a > b;
    }
};

int main() {
    vector<int> v = {1, 4, 2, 3};
    sort(v.begin(), v.end(), Desc());
}
```

👉 정렬 기준을 **객체로 전달**

---

## 6️⃣ Lambda와 Functor의 관계

람다는 **컴파일러가 자동으로 만들어주는 Functor**

```cpp
auto f = [x = 10](int a) {
    return a + x;
};
```

⬇ 내부적으로는 거의 이런 형태

```cpp
struct __Lambda {
    int x;
    int operator()(int a) const {
        return a + x;
    }
};
```

👉 **람다 = 익명 functor**

---

## 7️⃣ 언제 Functor를 쓰나?

✔ 상태가 필요한 콜백
✔ STL 알고리즘 커스터마이징
✔ 임베디드 / 성능 민감 코드
✔ 람다를 쓰기 어려운 복잡한 로직

---

## 한 줄 요약 🔥

> **Functor = `operator()`를 가진 객체 → 상태를 가진 함수**

원하면

* 임베디드 관점에서 왜 중요한지
* 함수 포인터 / 람다 / functor 비교 심화
* 실제 STL 내부에서 어떻게 쓰이는지

이런 것도 이어서 설명해줄게 👍



세 문장은 **겉보기엔 비슷하지만 “초기화 방식”이 다르고**,
그 차이가 **복사 생성자 / 이동 생성자 선택**과 직접 연결됩니다.
아래에서 **컴파일러가 어떤 생성자를 고르는지 기준으로** 설명할게요.

---

## 먼저 한 줄 요약

```cpp
Mat m1 = m2;   // copy initialization
Mat m1(m2);   // direct initialization
Mat m1{m2};   // list (brace) initialization
```

👉 **셋 다 결과는 같고(복사 생성자 호출)**
👉 **차이는 “초기화 문법”과 “생성자 선택 규칙”**

---

## 1️⃣ `Mat m1 = m2;`  (Copy initialization)

### 의미

> “m2로 m1을 초기화하라”

이름 때문에 헷갈리지만 ❌ **대입 연산자 아님**

### 실제 동작

```cpp
Mat m1(m2);   // 내부적으로 이렇게 처리됨
```

✔ **복사 생성자 호출**

```cpp
Mat(const Mat& other);
```

### 특징

* 가장 **자연스러운 C 스타일**
* **explicit 생성자**는 사용 ❌
* move / copy 선택은 **rvalue인지 여부**로 결정

```cpp
Mat m1 = Mat();     // 이동 생성자
Mat m2 = m1;        // 복사 생성자
```

---

## 2️⃣ `Mat m1(m2);`  (Direct initialization)

### 의미

> “m2를 인자로 해서 직접 생성자를 호출”

### 실제 동작

```cpp
Mat(const Mat& other);  // 정확히 이 생성자 호출
```

✔ **복사 생성자**
✔ `explicit` 생성자도 사용 가능

### 특징

* 가장 **정석적인 생성자 호출**
* 템플릿 / STL 내부에서 많이 사용
* 생성자 선택이 가장 **명확**

---

## 3️⃣ `Mat m1{m2};`  (Brace / List initialization)

### 의미

> “initializer list 규칙을 적용해서 초기화”

### 생성자 탐색 순서

1. `initializer_list` 생성자
2. 없으면 → 일반 생성자
3. narrowing conversion 체크

### 여기서는?

```cpp
Mat(const Mat& other);
```

✔ 역시 **복사 생성자 호출**

### 특징

* C++11 이후 권장 문법
* **의도치 않은 형 변환 방지**
* initializer_list가 있으면 **우선권** 가짐 ⚠️

---

## 🔁 move constructor와의 연결

### 핵심 기준은 단 하나

> **오른쪽 값이 rvalue인가?**

---

### 🔹 lvalue일 때 (m2)

```cpp
Mat m2;
Mat a = m2;
Mat b(m2);
Mat c{m2};
```

✔ 전부 **복사 생성자**

---

### 🔹 rvalue일 때

```cpp
Mat a = Mat();
Mat b(Mat());
Mat c{Mat()};
```

✔ 전부 **이동 생성자**

```cpp
Mat(Mat&& other);
```

---

### 🔹 `std::move` 사용 시

```cpp
Mat m2;
Mat a = std::move(m2);
Mat b(std::move(m2));
Mat c{std::move(m2)};
```

✔ 전부 **이동 생성자**

---

## 4️⃣ 표로 정리

| 문법                | 초기화 방식                | 생성자 선택 |
| ----------------- | --------------------- | ------ |
| `Mat m1 = m2;`    | copy initialization   | 복사 생성자 |
| `Mat m1(m2);`     | direct initialization | 복사 생성자 |
| `Mat m1{m2};`     | list initialization   | 복사 생성자 |
| `Mat m1 = Mat();` | copy init             | 이동 생성자 |
| `Mat m1(Mat());`  | direct init           | 이동 생성자 |
| `Mat m1{Mat()};`  | brace init            | 이동 생성자 |

---

## 5️⃣ 실무 관점에서 중요한 포인트

### ✔ 생성자 선택은 **문법보다 값의 종류(lvalue / rvalue)**가 핵심

### ✔ `=` 있다고 대입 연산자 아님

### ✔ `{}`는 initializer_list 때문에 **의도치 않은 생성자 호출 위험**

```cpp
Mat m1{10};   // initializer_list 생성자 호출될 수도 있음
```

---

## 🔚 한 문장 결론

> **이 세 문법의 차이는 “초기화 규칙”이고,
> 실제로 복사냐 이동이냐를 결정하는 건 “rvalue냐 아니냐”다.**

원하면

* OpenCV `cv::Mat`에서 실제로 어떤 일이 벌어지는지
* initializer_list 때문에 생기는 함정
* 생성자 오버로드 해석 순서

까지 이어서 설명해줄게 👍


응, 있어 👍
**C++에서는 생성자에서 “멤버 이니셜라이저 리스트(initializer list)”로 필드를 초기화하는 문법**을 많이 써.

---

## 기본 문법

```cpp
class Person {
    int age;
    double height;

public:
    Person(int a, double h)
        : age(a), height(h)   // ← 여기!
    {
        // 생성자 본문
    }
};
```

이 `: age(a), height(h)` 부분이 **필드 초기화 문법**이야.

---

## 왜 이 문법을 쓰는가?

### 1️⃣ 진짜 “초기화”이기 때문

아래 두 코드는 다르다.

```cpp
// ❌ 대입
Person(int a) {
    age = a;
}
```

```cpp
// ✅ 초기화
Person(int a) : age(a) {}
```

* 위: `age`가 **먼저 기본 초기화 → 그 다음 대입**
* 아래: `age`가 **한 번에 바로 초기화**

👉 성능 + 의미적으로 더 정확

---

### 2️⃣ 반드시 필요한 경우가 있음

아래 멤버들은 **이니셜라이저 리스트 없이는 불가능**해.

#### 🔹 const 멤버

```cpp
class A {
    const int x;
public:
    A(int v) : x(v) {}  // 필수
};
```

#### 🔹 reference 멤버

```cpp
class B {
    int& ref;
public:
    B(int& r) : ref(r) {}  // 필수
};
```

#### 🔹 기본 생성자가 없는 멤버

```cpp
class C {
    std::string s;
public:
    C(const std::string& str) : s(str) {}
};
```

---

### 3️⃣ 상속 시 부모 클래스 초기화

```cpp
class Base {
public:
    Base(int x) {}
};

class Derived : public Base {
public:
    Derived(int x) : Base(x) {}  // 부모 생성자 호출
};
```

➡️ **부모 클래스는 이니셜라이저 리스트에서만 초기화 가능**

---

## 초기화 순서 주의 ⚠️

```cpp
class Test {
    int a;
    int b;
public:
    Test() : b(2), a(1) {}
};
```

❗ 실제 초기화 순서:

1. `a`
2. `b`

➡️ **선언된 순서대로 초기화**, 이니셜라이저 리스트 순서와 무관
(컴파일러 경고 뜨는 경우 많음)

---

## 언제 쓰는 게 좋을까?

✅ **항상 쓰는 게 권장**

* 특히 POD가 아닌 타입 (`std::string`, `vector`, 커스텀 클래스`)
* const / reference
* 상속 구조

❌ 생성자 안에서 대입은 특별한 이유 없으면 피하는 게 좋아

---

원하면

* 임베디드 관점에서 왜 중요한지
* 생성자 초기화 vs `{}` 멤버 초기화(C++11) 비교
* 실제 면접에서 자주 나오는 포인트

이런 것도 이어서 설명해줄게.

오케이, 그럼 **② 생성자 멤버 이니셜라이저 vs `{}` 멤버 초기화(C++11 이후)** 정리해줄게.

---

## 1️⃣ `{}` 멤버 초기화 (in-class member initializer)

```cpp
class Foo {
    int a = 10;
    int b{20};
};
```

* **클래스 정의 시점**에 기본값을 지정
* 생성자를 안 만들어도 자동 적용됨

```cpp
Foo f;
```

➡️ `a = 10`, `b = 20`

---

## 2️⃣ 생성자 멤버 이니셜라이저

```cpp
class Foo {
    int a;
    int b;
public:
    Foo(int x, int y) : a(x), b(y) {}
};
```

* **생성자마다 다른 초기값**을 줄 수 있음
* 런타임 값으로 초기화 가능

---

## 3️⃣ 둘을 같이 쓰면 어떻게 될까?

```cpp
class Foo {
    int a = 10;
    int b = 20;
public:
    Foo(int x) : a(x) {}
};
```

결과:

* `a` → 생성자 이니셜라이저가 **우선**
* `b` → 클래스 `{}` 초기화 사용

➡️ `{}`는 **기본값**, 생성자는 **덮어쓰기**

---

## 4️⃣ 차이점 한눈에 비교

| 항목                | `{}` 멤버 초기화 | 생성자 이니셜라이저 |
| ----------------- | ----------- | ---------- |
| 도입                | C++11       | C++98      |
| 기본값 제공            | ✅           | ❌          |
| 생성자마다 다른 값        | ❌           | ✅          |
| const / reference | ✅           | ✅          |
| 가독성               | 좋음          | 상황에 따라     |

---

## 5️⃣ 언제 뭘 쓰는 게 좋을까?

### ✅ `{}` 멤버 초기화 추천 상황

* **항상 같은 기본값**
* 설정값, 플래그, 카운터
* 생성자가 여러 개인 클래스

```cpp
class Device {
    int retryCount = 3;
    bool enabled = true;
};
```

---

### ✅ 생성자 이니셜라이저 추천 상황

* 생성자 인자로 초기화해야 할 때
* 외부 값 의존
* 상속 / 의존 객체

```cpp
class Sensor {
    int id;
public:
    Sensor(int id) : id(id) {}
};
```

---

## 6️⃣ 임베디드/실무에서 중요한 포인트 ⚠️

* `{}` 멤버 초기화는 **컴파일 타임 상수** 느낌 → 안정적
* 생성자에서 대입(`=`)은 **초기화 + 대입**이라 비효율
* MCU 환경에서는
  👉 **초기화 순서 명확 + 불필요한 연산 제거**가 중요

---

## 7️⃣ 베스트 프랙티스 요약

> 🔹 **기본값은 `{}`**
> 🔹 **변하는 값은 생성자 이니셜라이저**
> 🔹 **생성자 본문에서 대입은 최후의 수단**

---

다음으로

* 생성자 여러 개 있을 때 초기화 전략
* `{}` vs `()` 차이 (narrowing, brace-init)
* 면접 단골 질문 정리

중 뭐 갈까?

